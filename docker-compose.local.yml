# Local development override — skips TLS, exposes ports directly
# Usage: docker compose -f docker-compose.yml -f docker-compose.local.yml up -d --build

services:
  caddy:
    volumes:
      - ./Caddyfile.local:/etc/caddy/Caddyfile:ro

  ui:
    ports:
      - "8501:8501"

  api:
    ports:
      - "8000:8000"
